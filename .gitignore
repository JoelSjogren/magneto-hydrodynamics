# Simulation outputs: ignore state dumps and rendered images, but keep
# videos and CSV time series / summaries (small, scientifically quantifying).
out/**
!out/**/
!out/videos/*.mp4
!out/videos/*.gif
!out/**/*.csv

Manifest.toml
